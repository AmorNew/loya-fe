import React, {useState} from "react";
import cn from 'classnames';

import styles from './ObjectCardTabs.module.css';


const ObjectCardTabs = ({state}: any) => {
    return (
        <div className={cn(styles.root)}>
            ObjectCardTabs
        </div>
    );
}

export default ObjectCardTabs;